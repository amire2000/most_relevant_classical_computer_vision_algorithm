# Classical Computer Vision with Python

An MkDocs course site for learning the most relevant classical computer-vision
algorithms with Python.

```bash
uv sync
uv run mkdocs serve
```

Build the static site with `uv run mkdocs build`.
